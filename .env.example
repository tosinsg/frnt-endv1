# Optional — Vite proxy already forwards /api → http://localhost:8080
# VITE_API_URL=http://localhost:8080/api

# Used only for building delivery URLs in the UI (uploads go through the backend)
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
